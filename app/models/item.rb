class Item
    @all = []
    def initialize(type, price)
        @type = type
        @price = price
        @all << self
    end

#     def add_to_cart(User, )
#         Cart.new(type, price, )
# end

# item.add_to_cart(cart_number, type, price)
#     Cart.all.find {|cart| cart.cart_number == Ca}

#     Cart.all.find {|cart| cart.customer == customer}
end
